---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: wa-notif-secrets
  namespace: apps
spec:
  encryptedData:
    # PLACEHOLDER - Replace with actual sealed secrets generated using kubeseal
    # Generate using: kubeseal --controller-namespace kube-system --controller-name sealed-secrets-controller --format yaml < secret.yaml
    # 
    # Optional secrets for wa-notif configuration:
    # - basic-auth: Format "username:password" for HTTP Basic Auth
    # - webhook-url: Webhook URL(s) for receiving events (comma-separated)
    # - webhook-secret: HMAC secret for webhook verification
    basic-auth: REPLACE_WITH_SEALED_BASIC_AUTH
    webhook-url: REPLACE_WITH_SEALED_WEBHOOK_URL
    webhook-secret: REPLACE_WITH_SEALED_WEBHOOK_SECRET
  template:
    metadata:
      name: wa-notif-secrets
      namespace: apps
